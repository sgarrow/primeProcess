Demonstrates the use Python’s multiprocessing module and it's
concurrent.futures module to run an application concurrently
across multiple CPU cores.  This can drastically reduce 
execution time for CPU-bound tasks.
